#include <move_waypoint/move_waypoint.h>

int main (int argc, char *argv[])
{   
    ros::init(argc, argv, "move_waypoint");
    Move_waypoints move_waypoint;
    //ros::spin();    
    
    return 0;
}